# Derek's Dot Files

My config files. I use OS X and Prezto (for Zsh).

## Installation

Run the following commands in your terminal.

```terminal
git clone git://github.com/dmorrison/dotfiles ~/.dotfiles
cd ~/.dotfiles
rake install
```
